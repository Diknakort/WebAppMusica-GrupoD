@model WebApplication_MUSICA_grupoD.Models.Colaboraciones

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Colaboraciones</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="GruposID" class="control-label"></label>
                <select asp-for="GruposID" class="form-control" asp-items="ViewBag.GruposID"></select>
                <span asp-validation-for="GruposID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ArtistasID" class="control-label"></label>
                <select asp-for="ArtistasID" class="form-control" asp-items="ViewBag.ArtistasID"></select>
                <span asp-validation-for="ArtistasID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
